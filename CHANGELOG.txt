v0.3.3 — UX + speed + crash fix
- Build button moved to the **bottom** of the page
- Faster sitemap discovery using robots.txt hints, concurrent HEAD probes, and caching
- Fixed crash by replacing `st.experimental_rerun()` with `st.rerun()`
- Keeps per-site checkboxes, concurrency, per-site page cap, and exclude filters
